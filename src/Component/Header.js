import NavBar from './NavBar';

function Header () {
    return (
        <header className={'header'}>
        <div className={'nav-area'}>
            <NavBar />
        </div>
        </header>
    );
};

export default Header;